name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: make_cmake
      run: |
        cmake .
        make
    - name: run
      run: ./World_Of_Waifus
        
